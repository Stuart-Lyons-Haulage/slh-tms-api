# Geofence API PR deployment trigger

This no-runtime-change marker is merged through a pull request so GitHub emits the normal `main` push event and redeploys the current API revision containing the Geofence update routes.
